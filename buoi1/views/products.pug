extends layout/abc.pug
include mixins/box-head.pug 

block main 
  //- img(src="/img/ptit.jpg", alt="")
  //- h1= message
  //- button(class="btn btn-success") click Me!
  +box-head("trang danh sách sản phẩm" , "Đây là thông tin mô tả trang danh sách sản phẩm")
  ul 
    each item in products
        li #{item.phone}